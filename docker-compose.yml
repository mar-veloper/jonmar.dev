version: '3'

services:
  portfolio:
    build:
      context: ./Dockerfiles
    container_name: jonmar.dev
    restart: 'unless-stopped'

networks:
  default:
    external:
      name: nginx-proxy-manager_default
